<p>{{ invite.createdBy }} sent invite for you into <a href="{{ url('homepage') }}">Scriptorium.cloud</a>. </p>
<p>Please follow link <a href="{{ url('invite_accept', {'id' : invite.id}) }}">{{ url('invite_accept', {'id' : invite.id}) }}</a></p>